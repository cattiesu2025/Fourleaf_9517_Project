# Python
.venv/
*.egg-info/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
logs/

# Local/editor files
.DS_Store
.idea/
.vscode/

# Raw datasets and generated artifacts are not versioned.
# Small shared metadata under data/metadata/ is intentionally tracked.
data/raw/**
!data/raw/.gitkeep
data/subset/**
outputs/**
!outputs/.gitkeep
demo/evaluation_mvp/expected_output/*.png
report/generated/*.png
report/generated/*.pdf

# Models and experiment logs are not versioned.
checkpoints/
*.ckpt
*.pth
*.pt
wandb/
runs/

# Local archives and reference material
scratch_outputs*.tar.gz
scratch_best*.tar.gz
docs/source/official/
docs/source/screenshots/

# Temporary PDF review artifacts
tmp/

# Local dependency/cache folders
.deps/

# Large local dataset archives. Keep metadata tracked.
data/**/*.zip
data/**/*.tar
data/**/*.tar.gz
/*.tar.gz

# Generated subset metadata and quality reports can be large; rebuild locally.
data/metadata/train_full.csv
data/metadata/full_train_paths.txt
data/metadata/split_config_full.json
data/metadata/train_full_clean.csv
data/metadata/data_quality_report.csv
data/metadata/data_quality_summary.json

# Development notes
docs/dev_log.md

# Local backup from replacing the old placeholder file with data/
data.placeholder
